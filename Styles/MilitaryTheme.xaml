<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework">

    <!-- Màu sắc nhận diện Quân đội Hiện đại (Military Navy & Forest & Slate) -->
    <Color x:Key="PrimaryColor">#1E3A8A</Color>
    <Color x:Key="PrimaryLightColor">#2563EB</Color>
    <Color x:Key="PrimaryDarkColor">#172554</Color>
    <Color x:Key="AccentColor">#15803D</Color>
    <Color x:Key="AccentLightColor">#22C55E</Color>
    <Color x:Key="WarningColor">#D97706</Color>
    <Color x:Key="DangerColor">#DC2626</Color>
    <Color x:Key="SuccessColor">#16A34A</Color>
    
    <Color x:Key="BgDarkColor">#0F172A</Color>
    <Color x:Key="BgLightColor">#F8FAFC</Color>
    <Color x:Key="BgCardColor">#FFFFFF</Color>
    <Color x:Key="BorderColor">#E2E8F0</Color>
    <Color x:Key="TextPrimaryColor">#0F172A</Color>
    <Color x:Key="TextSecondaryColor">#64748B</Color>

    <!-- ==================== BẢNG MÀU SEMANTIC DUAL-THEME CHUẨN ==================== -->
    <!-- Mặc định: Chế độ Tác chiến Quân đội (Combat Command Center - promt.txt & DemoUI.png) -->
    <SolidColorBrush x:Key="PageBackgroundBrush" Color="#101B14"/>
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1B2A1E"/>
    <SolidColorBrush x:Key="CardBorderBrush" Color="#625C34"/>
    <SolidColorBrush x:Key="KpiCardBackgroundBrush" Color="#18251A"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F7F1D4"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#B9B99E"/>
    <SolidColorBrush x:Key="TextDarkBrush" Color="#20241D"/>

    <SolidColorBrush x:Key="InputBackgroundBrush" Color="#EAE0BA"/>
    <SolidColorBrush x:Key="InputForegroundBrush" Color="#20241D"/>
    <SolidColorBrush x:Key="InputBorderBrush" Color="#8C7D46"/>

    <SolidColorBrush x:Key="PrimaryBrush" Color="#8F1515"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="#B32020"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#741010"/>
    <SolidColorBrush x:Key="PrimaryBorderBrush" Color="#D4B547"/>

    <SolidColorBrush x:Key="SecondaryButtonBackgroundBrush" Color="#253628"/>
    <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#8B7A3D"/>
    <SolidColorBrush x:Key="SecondaryButtonForegroundBrush" Color="#F7F1D4"/>

    <SolidColorBrush x:Key="AccentBrush" Color="#F2C94C"/>
    <SolidColorBrush x:Key="AccentLightBrush" Color="#FFE17A"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>

    <SolidColorBrush x:Key="BgDarkBrush" Color="#101B14"/>
    <SolidColorBrush x:Key="BgLightBrush" Color="#18251A"/>
    <SolidColorBrush x:Key="BgCardBrush" Color="#1B2A1E"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#625C34"/>

    <!-- Dynamic DataGrid Brushes -->
    <SolidColorBrush x:Key="DataGridHeaderBackgroundBrush" Color="#35452C"/>
    <SolidColorBrush x:Key="DataGridHeaderForegroundBrush" Color="#FFE17A"/>
    <SolidColorBrush x:Key="DataGridHeaderBorderBrush" Color="#625C34"/>
    <SolidColorBrush x:Key="DataGridRowBackgroundBrush" Color="#1B2A1E"/>
    <SolidColorBrush x:Key="DataGridRowAltBackgroundBrush" Color="#243424"/>
    <SolidColorBrush x:Key="DataGridRowForegroundBrush" Color="#F7F1D4"/>
    <SolidColorBrush x:Key="DataGridLineBrush" Color="#4F573E"/>
    <SolidColorBrush x:Key="DataGridSelectedBackgroundBrush" Color="#4E1215"/>
    <SolidColorBrush x:Key="DataGridSelectedForegroundBrush" Color="#FFE17A"/>

    <!-- TabHeader Brushes (Mặc định Chế độ Tác chiến) -->
    <SolidColorBrush x:Key="TabHeaderBackgroundBrush" Color="#253628"/>
    <SolidColorBrush x:Key="TabHeaderForegroundBrush" Color="#B9B99E"/>
    <SolidColorBrush x:Key="TabHeaderActiveBackgroundBrush" Color="#8F1515"/>
    <SolidColorBrush x:Key="TabHeaderActiveForegroundBrush" Color="#FFE17A"/>
    <SolidColorBrush x:Key="TabHeaderBorderBrush" Color="#625C34"/>

    <!-- Style cho Nút Bấm Chính (Primary Button) -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#B32020"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#F2C94C"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#741010"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style cho Nút Bấm Quân đội / Thành công (Success / Accent Button) -->
    <Style x:Key="SuccessButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#22C55E"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style cho Nút Bấm Phụ (Secondary / Outlined Button) -->
    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#354B38"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#A28D42"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style cho Nút Xóa / Danger Button -->
    <Style x:Key="DangerButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#B91C1C"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style cho TextBox Hiện đại & Tác chiến -->
    <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource InputForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBorderBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style cho PasswordBox Hiện đại & Tác chiến -->
    <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource InputForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBorderBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== COMBOBOX QUÂN ĐỘI KHAKI / PARCHMENT ==================== -->
    <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="28" />
            </Grid.ColumnDefinitions>
            <Border x:Name="Border" 
                    Grid.ColumnSpan="2"
                    CornerRadius="4"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}" />
            <Path x:Name="Arrow" 
                  Grid.Column="1" 
                  Fill="{TemplateBinding Foreground}"
                  HorizontalAlignment="Center" 
                  VerticalAlignment="Center"
                  Data="M 0 0 L 4 4 L 8 0 Z"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="#F2C94C"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Border" Property="BorderBrush" Value="#F2C94C"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ModernComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource InputForegroundBrush}"/>
        <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" 
                                      Template="{StaticResource ComboBoxToggleButton}" 
                                      Grid.Column="2" 
                                      Focusable="false"
                                      IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="{TemplateBinding Background}"
                                      Foreground="{TemplateBinding Foreground}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"/>
                        <ContentPresenter x:Name="ContentSite" 
                                          IsHitTestVisible="False" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="10,3,28,3"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left" />
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" 
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{DynamicResource InputBackgroundBrush}"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource InputBorderBrush}"
                                        CornerRadius="4"
                                        Margin="0,2,0,4">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="3" Opacity="0.3" Color="#000000"/>
                                    </Border.Effect>
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ModernComboBoxStyle}"/>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="10,7"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Foreground" Value="{DynamicResource InputForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="3">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#D5CDA9"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#8F1515"/>
                            <Setter Property="Foreground" Value="#FFE17A"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== PROGRESSBAR PHẲNG QUÂN ĐỘI ==================== -->
    <Style TargetType="{x:Type ProgressBar}">
        <Setter Property="Height" Value="18"/>
        <Setter Property="Background" Value="#EAE0BA"/>
        <Setter Property="Foreground" Value="#139D4B"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4" ClipToBounds="True">
                        <Grid x:Name="PART_Track">
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left" CornerRadius="4"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== DATEPICKER QUÂN ĐỘI KHAKI ==================== -->
    <Style TargetType="{x:Type primitives:DatePickerTextBox}">
        <Setter Property="Background" Value="#18251A"/>
        <Setter Property="Foreground" Value="#FFE17A"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type primitives:DatePickerTextBox}">
                    <Grid Background="{TemplateBinding Background}">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        <ContentControl x:Name="PART_Watermark" Focusable="False" Visibility="Collapsed" IsHitTestVisible="False" Width="0" Height="0"/>
                        <TextBlock Text="Chọn ngày" Foreground="#7C8D76" FontStyle="Italic"
                                   VerticalAlignment="Center" Margin="6,0,0,0" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=primitives:DatePickerTextBox}}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type DatePicker}">
        <Setter Property="Foreground" Value="#FFE17A"/>
        <Setter Property="Background" Value="#18251A"/>
        <Setter Property="BorderBrush" Value="#8C7D46"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="4,0"/>
    </Style>

    <!-- ==================== SCROLLBAR QUÂN ĐỘI (CHỐNG VẠCH TRẮNG) ==================== -->
    <Style x:Key="MilitaryScrollBarThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="thumbBorder"
                            Background="#35452C"
                            CornerRadius="3"
                            Margin="1"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="thumbBorder" Property="Background" Value="#8C7D46"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="true">
                            <Setter TargetName="thumbBorder" Property="Background" Value="#F2C94C"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="SnapsToDevicePixels" Value="true"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="true">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource MilitaryScrollBarThumbStyle}"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource MilitaryScrollBarThumbStyle}"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Style Thẻ Card (Dynamic Dual-Theme) -->
    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" Color="#000000" Opacity="0.15" ShadowDepth="2" Direction="270"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Hiệu ứng đổ bóng CardShadow & ShadowEffect dùng chung -->
    <DropShadowEffect x:Key="CardShadow" BlurRadius="12" Color="#000000" Opacity="0.2" ShadowDepth="3"/>
    <DropShadowEffect x:Key="ShadowEffect" BlurRadius="12" Color="#000000" Opacity="0.2" ShadowDepth="3"/>

    <!-- Style Header cho TabItem (Tác chiến & Hành chính) -->
    <Style x:Key="TabHeaderStyle" TargetType="TabItem">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="16,9"/>
        <Setter Property="Foreground" Value="{DynamicResource TabHeaderForegroundBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder" Background="Transparent" BorderBrush="Transparent" BorderThickness="0,0,0,3" Margin="0,0,8,0" Padding="{TemplateBinding Padding}" Cursor="Hand">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource TabHeaderActiveBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TabHeaderActiveForegroundBrush}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource TabHeaderBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== DATAGRID QUÂN ĐỘI: ZEBRA STRIPING DUAL-THEME ==================== -->
    <!-- Triệt tiêu hoàn toàn viền trắng RowHeader và nút SelectAll ở góc trái DataGrid -->
    <Style TargetType="DataGridRowHeader">
        <Setter Property="Width" Value="0"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MaxWidth" Value="0"/>
        <Setter Property="Visibility" Value="Collapsed"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style TargetType="{x:Type Button}" x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type DataGrid}, ResourceId=DataGridSelectAllButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="0"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MaxWidth" Value="0"/>
        <Setter Property="Visibility" Value="Collapsed"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MilitaryDataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="RowHeight" Value="38"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="RowBackground" Value="{DynamicResource DataGridRowBackgroundBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridRowAltBackgroundBrush}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGridLineBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGridRowForegroundBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="RowHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridRowHeader">
                    <Setter Property="Width" Value="0"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="MaxWidth" Value="0"/>
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGrid" BasedOn="{StaticResource MilitaryDataGridStyle}"/>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderForegroundBrush}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DataGridHeaderBorderBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="SeparatorBrush" Value="{DynamicResource DataGridHeaderBorderBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                <Setter Property="Background" Value="{DynamicResource DataGridRowBackgroundBrush}"/>
            </Trigger>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{DynamicResource DataGridRowAltBackgroundBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#354B38"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGridSelectedBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGridSelectedForegroundBrush}"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource DataGridSelectedForegroundBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ================================================================= -->
    <!-- DESIGN SYSTEM QUÂN ĐỘI NHÂN DÂN VIỆT NAM (PROMT.TXT & DEMOUI.PNG) -->
    <!-- ================================================================= -->

    <!-- Core Palette Colors -->
    <Color x:Key="MilBackground">#101B14</Color>
    <Color x:Key="MilTopBarDark">#09130E</Color>
    <Color x:Key="MilSidebar">#132018</Color>
    <Color x:Key="MilSidebar2">#18291D</Color>

    <Color x:Key="MilSurface">#243424</Color>
    <Color x:Key="MilSurface2">#2B3D2B</Color>
    <Color x:Key="MilSurface3">#314631</Color>
    <Color x:Key="MilSurfaceLight">#E5DCB9</Color>
    <Color x:Key="MilInputBg">#EAE0BA</Color>

    <Color x:Key="MilRed">#8F1515</Color>
    <Color x:Key="MilRedHover">#B32020</Color>
    <Color x:Key="MilRedDark">#741010</Color>
    <Color x:Key="MilBurgundy">#4E1215</Color>

    <Color x:Key="MilGold">#F2C94C</Color>
    <Color x:Key="MilGoldLight">#FFE17A</Color>
    <Color x:Key="MilGoldDark">#B89432</Color>

    <Color x:Key="MilOlive">#35452C</Color>
    <Color x:Key="MilOliveLight">#4A5C3B</Color>
    <Color x:Key="MilOliveDark">#18251A</Color>

    <Color x:Key="MilText">#F7F1D4</Color>
    <Color x:Key="MilTextMuted">#B9B99E</Color>
    <Color x:Key="MilTextDark">#20241D</Color>
    <Color x:Key="MilTextDisabled">#747769</Color>

    <Color x:Key="MilBorder">#625C34</Color>
    <Color x:Key="MilBorderHigh">#A28D42</Color>
    <Color x:Key="MilBorderActive">#F2C94C</Color>

    <Color x:Key="MilSuccess">#218C45</Color>
    <Color x:Key="MilSuccessLight">#36C567</Color>
    <Color x:Key="MilDanger">#B92929</Color>
    <Color x:Key="MilWarning">#D08A1B</Color>
    <Color x:Key="MilInfo">#2563D9</Color>
    <Color x:Key="MilExcellent">#7546C7</Color>

    <!-- Core Palette Brushes -->
    <SolidColorBrush x:Key="MilBackgroundBrush" Color="{StaticResource MilBackground}"/>
    <SolidColorBrush x:Key="MilTopBarDarkBrush" Color="{StaticResource MilTopBarDark}"/>
    <SolidColorBrush x:Key="MilSidebarBrush" Color="{StaticResource MilSidebar}"/>
    <SolidColorBrush x:Key="MilSidebar2Brush" Color="{StaticResource MilSidebar2}"/>
    <SolidColorBrush x:Key="MilSurfaceBrush" Color="{StaticResource MilSurface}"/>
    <SolidColorBrush x:Key="MilSurface2Brush" Color="{StaticResource MilSurface2}"/>
    <SolidColorBrush x:Key="MilSurface3Brush" Color="{StaticResource MilSurface3}"/>
    <SolidColorBrush x:Key="MilSurfaceLightBrush" Color="{StaticResource MilSurfaceLight}"/>
    <SolidColorBrush x:Key="MilInputBgBrush" Color="{StaticResource MilInputBg}"/>
    <SolidColorBrush x:Key="MilRedBrush" Color="{StaticResource MilRed}"/>
    <SolidColorBrush x:Key="MilRedHoverBrush" Color="{StaticResource MilRedHover}"/>
    <SolidColorBrush x:Key="MilRedDarkBrush" Color="{StaticResource MilRedDark}"/>
    <SolidColorBrush x:Key="MilBurgundyBrush" Color="{StaticResource MilBurgundy}"/>
    <SolidColorBrush x:Key="MilGoldBrush" Color="{StaticResource MilGold}"/>
    <SolidColorBrush x:Key="MilGoldLightBrush" Color="{StaticResource MilGoldLight}"/>
    <SolidColorBrush x:Key="MilGoldDarkBrush" Color="{StaticResource MilGoldDark}"/>
    <SolidColorBrush x:Key="MilOliveBrush" Color="{StaticResource MilOlive}"/>
    <SolidColorBrush x:Key="MilOliveLightBrush" Color="{StaticResource MilOliveLight}"/>
    <SolidColorBrush x:Key="MilOliveDarkBrush" Color="{StaticResource MilOliveDark}"/>
    <SolidColorBrush x:Key="MilTextBrush" Color="{StaticResource MilText}"/>
    <SolidColorBrush x:Key="MilTextMutedBrush" Color="{StaticResource MilTextMuted}"/>
    <SolidColorBrush x:Key="MilTextDarkBrush" Color="{StaticResource MilTextDark}"/>
    <SolidColorBrush x:Key="MilTextDisabledBrush" Color="{StaticResource MilTextDisabled}"/>
    <SolidColorBrush x:Key="MilBorderBrush" Color="{StaticResource MilBorder}"/>
    <SolidColorBrush x:Key="MilBorderHighBrush" Color="{StaticResource MilBorderHigh}"/>
    <SolidColorBrush x:Key="MilBorderActiveBrush" Color="{StaticResource MilBorderActive}"/>
    <SolidColorBrush x:Key="MilSuccessBrush" Color="{StaticResource MilSuccess}"/>
    <SolidColorBrush x:Key="MilSuccessLightBrush" Color="{StaticResource MilSuccessLight}"/>
    <SolidColorBrush x:Key="MilDangerBrush" Color="{StaticResource MilDanger}"/>
    <SolidColorBrush x:Key="MilWarningBrush" Color="{StaticResource MilWarning}"/>
    <SolidColorBrush x:Key="MilInfoBrush" Color="{StaticResource MilInfo}"/>
    <SolidColorBrush x:Key="MilExcellentBrush" Color="{StaticResource MilExcellent}"/>

    <!-- Gradients -->
    <LinearGradientBrush x:Key="MilTopBarGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#5A0E10" Offset="0.0"/>
        <GradientStop Color="#8F1515" Offset="0.5"/>
        <GradientStop Color="#5A0E10" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="MilActiveMenuGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#8F1515" Offset="0.0"/>
        <GradientStop Color="#B32020" Offset="1.0"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="MilBannerGradient" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#4E1215" Offset="0.0"/>
        <GradientStop Color="#741010" Offset="0.5"/>
        <GradientStop Color="#4E1215" Offset="1.0"/>
    </LinearGradientBrush>

    <!-- Nút Bấm Tác Chiến Đỏ Mạ Vàng (Combat Primary Button) -->
    <Style x:Key="CombatPrimaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource MilRedBrush}"/>
        <Setter Property="Foreground" Value="#FFFFFF"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilGoldLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="btnBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource MilRedHoverBrush}"/>
                            <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource MilGoldBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource MilRedDarkBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Nút Bấm Tác Chiến Ô-liu Viền Đồng (Combat Secondary Button) -->
    <Style x:Key="CombatSecondaryButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource MilSurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MilTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="btnBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource MilSurface2Brush}"/>
                            <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource MilBorderHighBrush}"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource MilOliveDarkBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Ô Nhập Liệu Giấy Quân Lệnh (Combat Parchment TextBox) -->
    <Style x:Key="CombatParchmentTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource MilInputBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MilTextDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource MilGoldBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            <Setter Property="Background" Value="#FFF6D8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Giấy Quân Lệnh (Combat Parchment ComboBox) -->
    <Style x:Key="CombatParchmentComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource MilInputBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MilTextDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,6"/>
    </Style>

    <!-- Khung Thẻ Tác Chiến Quân Đội (Combat Card Border) -->
    <Style x:Key="CombatCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource MilSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

    <!-- Khung Thẻ KPI Tác Chiến (Combat KPI Card) -->
    <Style x:Key="CombatKpiCardBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource MilOliveDarkBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MilBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="14,12"/>
    </Style>

</ResourceDictionary>
