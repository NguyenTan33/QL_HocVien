<Window x:Class="QL_HocVien.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:QL_HocVien.ViewModels"
        xmlns:views="clr-namespace:QL_HocVien.Views.UserControls"
        xmlns:local_cv="clr-namespace:QL_HocVien.Converters"
        mc:Ignorable="d"
        Title="Hệ Thống Quản Lý Học Viên Quân Đội"
        Icon="pack://application:,,,/QL_HocVien;component/Resources/app_icon.ico"
        Height="800" Width="1280"
        MinHeight="680" MinWidth="1050"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#F1F5F9">

    <Window.Resources>
        <!-- DataTemplates liên kết từng ViewModel với UserControl tương ứng (MVVM) -->
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CadetManagementViewModel}">
            <views:CadetManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClassManagementViewModel}">
            <views:ClassManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AddCadetViewModel}">
            <views:AddCadetView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SubjectManagementViewModel}">
            <views:SubjectManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PhysicalExamViewModel}">
            <views:PhysicalExamView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OfficerManagementViewModel}">
            <views:OfficerManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CatalogManagementViewModel}">
            <views:CatalogManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CreditSubjectManagementViewModel}">
            <views:CreditSubjectManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AcademicAnalyticsViewModel}">
            <views:AcademicAnalyticsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ExamAnalyticsViewModel}">
            <views:ExamAnalyticsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TrainingTimelineViewModel}">
            <views:TrainingTimelineView/>
        </DataTemplate>

        <!-- Style cho các nút điều hướng Sidebar (Tự động Active theo Chế độ Tác chiến / Hành chính) -->
        <Style x:Key="SidebarNavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SidebarNavForegroundBrush}"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="8,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="navBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Trạng thái Rê chuột (Hover) -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SidebarNavHoverBackgroundBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource SidebarNavHoverBorderBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                </Trigger>
                <!-- Trạng thái Đang được chọn (Active Menu - Đổi theo Chế độ Tác chiến / Hành chính) -->
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <MultiBinding Converter="{x:Static local_cv:MenuMatchToBooleanConverter.Instance}">
                            <Binding Path="ActiveMenu"/>
                            <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Background" Value="{DynamicResource ActiveMenuBackgroundBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ActiveMenuBorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource ActiveMenuForegroundBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- 0. TopBar Quân kỳ & Khẩu hiệu -->
            <RowDefinition Height="40"/>
            <!-- 1. Vùng làm việc chính -->
            <RowDefinition Height="*"/>
            <!-- 2. Khẩu hiệu chân trang (Bottom Banner) -->
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <!-- ==================== 0. CUSTOM TOPBAR ==================== -->
        <Border Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                Background="{DynamicResource TopBarBackgroundBrush}"
                BorderBrush="{DynamicResource TopBarBorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid Margin="14,0,8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Trái: Quân hiệu & Tiêu đề hệ thống -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Biểu tượng cho Chế độ Tác chiến: Ngôi sao vàng -->
                    <TextBlock Text="★" FontSize="15" FontWeight="Bold" Margin="0,0,8,0" VerticalAlignment="Center"
                               Foreground="{DynamicResource AccentBrush}"
                               Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}"/>
                    
                    <!-- Biểu tượng cho Chế độ Hành chính: Logo Quân đội tách nền trong suốt -->
                    <Image Source="/Assets/Images/military_logo_transparent.png" 
                           Height="22" Width="19" Stretch="Uniform" Margin="0,0,8,0" VerticalAlignment="Center"
                           Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:InvertedBooleanToVisibilityConverter.Instance}}"/>

                    <TextBlock Text="QUÂN ĐỘI NHÂN DÂN VIỆT NAM  |" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"
                               Foreground="{DynamicResource TopBarTitleForegroundBrush}"/>
                    <TextBlock Text="  HỆ THỐNG QUẢN LÝ HỌC VIÊN " FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"
                               Foreground="{DynamicResource TopBarSubForegroundBrush}"/>
                </StackPanel>

                <!-- Giữa: Khẩu hiệu truyền thống -->
                <TextBlock Grid.Column="1" Text="“VÌ NHÂN DÂN PHỤC VỤ”" 
                           FontSize="11.5" FontWeight="ExtraBold" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0"
                           Foreground="{DynamicResource TopBarTitleForegroundBrush}"/>

                <!-- Phải: Nút Gạt Dual-Theme + Khóa Bảo Mật + Điều Khiển Cửa Sổ -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Nút Gạt Chuyển Đổi 2 Giao Diện (Dual-Theme) -->
                    <Button Command="{Binding ToggleThemeModeCommand}" ToolTip="{Binding ThemeModeTooltip}"
                            Height="26" Padding="10,0" Margin="0,0,12,0" Cursor="Hand" BorderThickness="1"
                            Background="{DynamicResource SecondaryButtonBackgroundBrush}"
                            BorderBrush="{DynamicResource SecondaryButtonBorderBrush}"
                            Foreground="{DynamicResource TextPrimaryBrush}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="🔄 " FontSize="10"/>
                            <TextBlock Text="{Binding ThemeModeButtonText}" FontSize="10" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>

                    <!-- Widget Khóa bảo mật -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0"
                                Visibility="{Binding IsSecurityProtectionEnabled, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                        <!-- Trạng thái: Đang mở khóa (có đếm ngược) -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                    Visibility="{Binding IsSecurityUnlocked, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                            <Border Background="#064E3B" BorderBrush="#059669" BorderThickness="1" CornerRadius="4" Padding="8,2" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🔓" FontSize="11" Margin="0,0,4,0"/>
                                    <TextBlock Text="MỞ KHÓA: " FontSize="10" FontWeight="Bold" Foreground="#34D399"/>
                                    <TextBlock Text="{Binding SecurityRemainingTime}" FontSize="11" FontWeight="ExtraBold" Foreground="#6EE7B7"/>
                                </StackPanel>
                            </Border>
                            <Button Content="🔒 Khóa Ngay" Height="24" Padding="8,0" Background="#DC2626" Foreground="White" BorderThickness="0"
                                    FontSize="10" FontWeight="Bold" Cursor="Hand" ToolTip="Lập tức khóa màn hình để bảo vệ dữ liệu khi rời máy"
                                    Command="{Binding QuickLockCommand}">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="4"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>

                        <!-- Trạng thái: Đang khóa (bấm để mở khóa) -->
                        <Button Background="{DynamicResource SecondaryButtonBackgroundBrush}" 
                                BorderBrush="{DynamicResource SecondaryButtonBorderBrush}" BorderThickness="1" Padding="8,2" Height="24" Cursor="Hand"
                                ToolTip="{Binding SecurityStatusTooltip}"
                                Command="{Binding QuickUnlockCommand}"
                                Visibility="{Binding IsSecurityUnlocked, Converter={x:Static local_cv:InvertedBooleanToVisibilityConverter.Instance}}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="🔒" FontSize="10" Margin="0,0,4,0"/>
                                <TextBlock Text="KHÓA BẢO MẬT (CHỈ XEM)" FontSize="10" FontWeight="Bold" Foreground="#D97706"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Các nút điều khiển cửa sổ -->
                    <Button Content="—" Width="34" Height="28" Background="Transparent" Foreground="{DynamicResource TopBarSubForegroundBrush}" BorderThickness="0"
                            Cursor="Hand" Click="BtnMinimize_Click"/>
                    <Button x:Name="BtnMax" Content="☐" Width="34" Height="28" Background="Transparent" Foreground="{DynamicResource TopBarSubForegroundBrush}" BorderThickness="0"
                            Cursor="Hand" Click="BtnMaximize_Click"/>
                    <Button Content="✕" Width="34" Height="28" Background="Transparent" Foreground="{DynamicResource TopBarSubForegroundBrush}" BorderThickness="0"
                            Cursor="Hand" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ==================== 1. THÂN ỨNG DỤNG ==================== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!-- Cột Sidebar bên trái chuẩn 270px như DemoUI -->
                <ColumnDefinition Width="270"/>
                <!-- Cột Nội dung chính -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR BÊN TRÁI -->
            <Border Grid.Column="0"
                    Background="{DynamicResource SidebarBackgroundBrush}"
                    BorderBrush="{DynamicResource SidebarBorderBrush}"
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Header Sidebar: Quân hiệu & Biểu trưng QĐNDVN -->
                        <RowDefinition Height="Auto"/>
                        <!-- Menu Links -->
                        <RowDefinition Height="*"/>
                        <!-- Footer / Thông tin người dùng đăng nhập -->
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header Sidebar: Tác chiến (Quân hiệu DemoUI) / Hành chính (Hiện đại sáng với Logo tách nền) -->
                    <Border Grid.Row="0" Height="168"
                            Background="{DynamicResource SidebarBackgroundBrush}"
                            BorderBrush="{DynamicResource SidebarBorderBrush}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <!-- Chế độ Tác chiến: Banner quân hiệu camo DemoUI -->
                            <Image Source="/Assets/Images/sidebar_header.png" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}"/>
                            
                            <!-- Chế độ Hành chính: Header sáng hiện đại với Logo Quân đội tách nền trong suốt -->
                            <Border Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:InvertedBooleanToVisibilityConverter.Instance}}"
                                    Padding="16,12" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel HorizontalAlignment="Center">
                                    <Image Source="/Assets/Images/military_logo_transparent.png" 
                                           Width="58" Height="68" Stretch="Uniform" HorizontalAlignment="Center" Margin="0,0,0,6">
                                        <Image.Effect>
                                            <DropShadowEffect BlurRadius="8" Color="#DC2626" Opacity="0.25" ShadowDepth="1.5"/>
                                        </Image.Effect>
                                    </Image>
                                    <TextBlock Text="QUÂN ĐỘI NHÂN DÂN VIỆT NAM" FontSize="11" FontWeight="Bold" Foreground="#DC2626" HorizontalAlignment="Center"/>
                                    <TextBlock Text="HỆ THỐNG QUẢN LÝ HỌC VIÊN" FontSize="11" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Menu Chức Năng Sidebar (Không thanh cuộn trắng, cuộn mượt bằng chuột) -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="0,2" PanningMode="VerticalOnly">
                        <StackPanel>
                            <!-- NHÓM 1: HỆ THỐNG QUẢN LÝ -->
                            <Border BorderThickness="0,0,0,1" Margin="12,4,12,2" Padding="0,0,0,2"
                                    BorderBrush="{DynamicResource SidebarBorderBrush}">
                                <TextBlock Text="HỆ THỐNG QUẢN LÝ" FontSize="10" FontWeight="Bold"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Border>

                            <!-- 1. Bảng Tổng Quan -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="Dashboard"
                                    Command="{Binding NavigateToDashboardCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="⌂" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,-2,8,0"/>
                                    <TextBlock Text="Bảng Tổng Quan" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 2. Quản Lý Cán Bộ (Chỉ Quản Trị Viên) -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="OfficerManagement"
                                    Command="{Binding NavigateToOfficerManagementCommand}"
                                    Visibility="{Binding IsAdmin, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="★" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Quản Lý Cán Bộ" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 3. Quản Lý Học Viên -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="CadetManagement"
                                    Command="{Binding NavigateToCadetManagementCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="👤" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Quản Lý Học Viên" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 4. Quản Lý Lớp Học -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="ClassManagement"
                                    Command="{Binding NavigateToClassManagementCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="👥" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Quản Lý Lớp Học" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 5. Danh Mục Tổ Chức (Đại Đội, Cấp Bậc, Chức Vụ - Chỉ Quản Trị Viên) -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="CatalogManagement"
                                    Command="{Binding NavigateToCatalogManagementCommand}"
                                    Visibility="{Binding IsAdmin, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📖" FontSize="13" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Danh Mục &amp; Đại Đội" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 6. Môn Học -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="CreditSubjectManagement"
                                    Command="{Binding NavigateToCreditSubjectManagementCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📖" FontSize="13" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Môn Học" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 7. Phân Tích Học Lực -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="AcademicAnalytics"
                                    Command="{Binding NavigateToAcademicAnalyticsCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📊" FontSize="13" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Phân Tích Học Lực" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 8. Tiêu Chuẩn Môn Thể Lực -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="SubjectManagement"
                                    Command="{Binding NavigateToSubjectManagementCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🏆" FontSize="13" Foreground="{DynamicResource SidebarIconBrush}" Width="22" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Tiêu Chuẩn Môn Thể Lực" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- NHÓM 2: RÈN LUYỆN & HỆ THỐNG -->
                            <Border BorderThickness="0,0,0,1" Margin="12,5,12,2" Padding="0,0,0,2"
                                    BorderBrush="{DynamicResource SidebarBorderBrush}">
                                <TextBlock Text="RÈN LUYỆN &amp; HỆ THỐNG" FontSize="10" FontWeight="Bold"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Border>

                            <!-- 9. Rèn Luyện Thể Lực (Chuẩn TT 32) -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="PhysicalExam"
                                    Command="{Binding NavigateToPhysicalExamCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="🏃" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="24" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Rèn Luyện Thể Lực" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 10. Phân Tích Thể Lực -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="ExamAnalytics"
                                    Command="{Binding NavigateToExamAnalyticsCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📈" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="24" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Phân Tích Thể Lực" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 11. Lịch Huấn Luyện & Mốc Sự Kiện -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="TrainingTimeline"
                                    Command="{Binding NavigateToTrainingTimelineCommand}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="📅" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="24" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Lịch Huấn Luyện &amp; Sự Kiện" FontSize="12.5"/>
                                </StackPanel>
                            </Button>

                            <!-- 12. Cài Đặt & Bảo Mật (Chỉ Quản Trị Viên) -->
                            <Button Style="{StaticResource SidebarNavButtonStyle}" Tag="Settings"
                                    Command="{Binding NavigateToSettingsCommand}"
                                    Visibility="{Binding IsAdmin, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="⚙" FontSize="14" Foreground="{DynamicResource SidebarIconBrush}" Width="24" TextAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="Cài Đặt" FontSize="12.5"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer Sidebar: Người Dùng & Đăng Xuất -->
                    <Border Grid.Row="2" Padding="12,6" BorderThickness="0,1,0,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#0C150E"/>
                                <Setter Property="BorderBrush" Value="#1F3022"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCombatMode}" Value="False">
                                        <Setter Property="Background" Value="#F8FAFC"/>
                                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Phù hiệu quân hàm 3 vạch vàng -->
                            <Image Grid.Column="0" Source="/Assets/Images/user_rank_badge.png" Width="34" Height="34" Margin="0,0,8,0"/>

                            <!-- Tên và vai trò -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding CurrentUser.FullName, FallbackValue='Nguyễn Lê Quốc An'}" 
                                           FontWeight="Bold" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                <Border Background="#168A40" CornerRadius="4" Padding="7,2" HorizontalAlignment="Left" Margin="0,3,0,0">
                                    <TextBlock Text="{Binding CurrentUser.Role, FallbackValue='Admin'}" FontSize="9.5" FontWeight="Bold" Foreground="#FFFFFF"/>
                                </Border>
                            </StackPanel>

                            <!-- Nút Đăng xuất cửa đỏ chuẩn DemoUI -->
                            <Button Grid.Column="2" Width="26" Height="30" Background="Transparent" BorderThickness="0"
                                    Cursor="Hand" ToolTip="Đăng Xuất" VerticalAlignment="Center"
                                    Command="{Binding LogoutCommand}">
                                <Image Source="/Assets/Images/logout_icon.png" Width="18" Height="22" Stretch="Uniform"/>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- VÙNG HIỂN THỊ NỘI DUNG CHÍNH (CONTENT AREA) -->
            <Border Grid.Column="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource MilBackgroundBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCombatMode}" Value="False">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <!-- Xe tăng & Đoàn quân tiến công ở đáy màn hình (Panel.ZIndex="0" để nằm phía dưới bảng dữ liệu) -->
                    <Image Source="/Assets/Images/tank_silhouette_peek.png" 
                           Panel.ZIndex="0"
                           Height="46" HorizontalAlignment="Left" VerticalAlignment="Bottom" Stretch="Uniform" 
                           Margin="15,0,0,0" IsHitTestVisible="False" Opacity="0.95"
                           Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}"/>

                    <!-- Vùng hiển thị nội dung bảng dữ liệu (Panel.ZIndex="1" để đè lên ảnh bộ đội) -->
                    <ContentControl Content="{Binding CurrentView}" Panel.ZIndex="1"/>
                </Grid>
            </Border>
        </Grid>

        <!-- ==================== 2. KHẨU HIỆU CHÂN TRANG / THANH CÔNG VỤ ==================== -->
        <Border Grid.Row="2" Height="34">
            <Grid>
                <!-- Chế độ Tác chiến: Dải khẩu hiệu quân đội dã chiến chuẩn DemoUI -->
                <Grid Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:BooleanToVisibilityConverter.Instance}}">
                    <Image Source="/Assets/Images/bottom_slogan_bar.png" Stretch="Fill" VerticalAlignment="Center"/>
                    <TextBlock Text="“XÂY DỰNG QUÂN ĐỘI NHÂN DÂN CÁCH MẠNG - CHÍNH QUY - TINH NHUỆ - HIỆN ĐẠI”"
                               FontSize="12" FontWeight="ExtraBold" Foreground="#FFE17A"
                               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="320,0,320,0"
                               TextTrimming="CharacterEllipsis">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="5" ShadowDepth="1.5" Opacity="0.9"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </Grid>

                <!-- Chế độ Hành chính: Thanh công vụ hiện đại, sáng -->
                <Border Visibility="{Binding IsCombatMode, Converter={x:Static local_cv:InvertedBooleanToVisibilityConverter.Instance}}"
                        Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="16,0">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="● " Foreground="#16A34A" FontSize="9"/>
                            <TextBlock Text="HỆ THỐNG SẴN SÀNG" FontSize="11" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="QUẢN TRỊ CÔNG TÁC HỌC VIÊN" 
                                   FontSize="11" FontWeight="SemiBold" Foreground="#64748B" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
